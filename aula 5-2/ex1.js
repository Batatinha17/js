// Vetor de três números inteiros. Multiplique cada elemento por 2 e exiba o vetor resultante

let num = [2, 4, 8]
let mult = []

for (let i = 0; i < num.length; i++) {
    mult[i] = num[i] * 2 
}
console.log(mult)
